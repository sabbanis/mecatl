apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mecabroker.fullname" . }}-config
  labels: {{- include "mecabroker.labels" . | nindent 4 }}
data:
  broker.json: |
{{ include "mecabroker.brokerConfig" . | indent 4 }}
